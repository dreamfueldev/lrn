{
  "name": "lrn",
  "version": "0.0.1",
  "description": "Universal CLI for learning and querying programming interfaces",
  "type": "module",
  "bin": {
    "lrn": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target node --packages external && echo '#!/usr/bin/env node' | cat - dist/index.js > dist/index.tmp.js && mv dist/index.tmp.js dist/index.js && chmod +x dist/index.js",
    "dev": "bun --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
